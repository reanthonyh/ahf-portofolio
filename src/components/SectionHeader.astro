---
interface Props {
  label: string;
  title: string;
}

const { label, title } = Astro.props;
---

<p class="text-emerald-400 font-share-tech-mono text-sm mb-2 tracking-wider uppercase">{label}</p>
<h3 class="text-2xl font-bold text-white mb-10">{title}</h3>
